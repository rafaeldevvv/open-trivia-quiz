export default function Timer({ timeout }) {
  return (
    <div
      style={{
        backgroundImage: ``,
        borderRadius: "50%",
        width: "20px",
        aspectRatio: "1",
      }}
    ></div>
  );
}
